<div class="tray_area">
  <% if session[:id] %>
      <% if @game && @game.players.include?(Player.find(session[:id])) %>
          <%= render partial: 'shared/tray' %>
      <% end %>
  <% else %>
      <%= render partial: 'shared/login' %>
  <% end %>
</div>